{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$defs": {
    "CourtCode": {
      "title": "Court Code",
      "type": "string",
      "pattern": "^[A-Za-z]{2,}(-[A-Za-z0-9]+)*$"
    }
  },
  "type": "object",
  "title": "Raw Court",
  "properties": {
    "code": {
      "title": "Court Code",
      "description": "A unique code (within Find Case Law) to identify this court entity. Courts which exist only as internal proxy entities should be prefixed with `TNA`.",
      "$ref": "./court.schema.json#/$defs/CourtCode"
    },
    "name": {
      "title": "Name",
      "description": "The name of this court as it will commonly appear on Find Case Law.",
      "type": "string"
    },
    "long_name": {
      "title": "Long Name",
      "description": "Optionally, the formal name of this court where it differs from the `name`.",
      "type": "string"
    },
    "grouped_name": {
      "type": "string"
    },
    "param": {
      "title": "Search parameter",
      "description": "The primary value for the `court` parameter to use when searching for this court.",
      "type": "string",
      "pattern": "^[a-z]{2,}(?:/[a-z0-9]+)?$"
    },
    "extra_params": {
      "title": "Extra parameters",
      "description": "A list of additional parameters which, if they appear as a `court` parameter in search, will be mapped to this court.",
      "type": "array",
      "items": {
        "type": "string",
        "pattern": "^[a-z]{2,}(/[a-z]+)?$"
      }
    },
    "ncn_pattern": {
      "title": "Neutral Citation Pattern",
      "description": "A regular expression pattern which matches valid NCNs from this court.",
      "type": "string"
    },
    "ncn_examples": {
      "title": "Neutral Citation examples",
      "description": "An array of example NCNs for this court.",
      "type": "array",
      "items": {
        "title": "Neutral Citation Number",
        "description": "An example Neutral Citation Number, matching our expected generic NCN pattern.",
        "type": "string",
        "pattern": "^\\[([0-9]{4})\\] ([a-zA-Z]+)(?: ([a-zA-Z]+))? ([0-9]+)(?: \\(([a-zA-Z0-9]+)\\))?$"
      }
    },
    "link": {
      "type": "string",
      "format": "uri"
    },
    "identifier_iri": {
      "type": "string",
      "format": "uri"
    },
    "start_year": {
      "title": "Start year",
      "description": "The default start year of the document holding range for this court in Find Case Law.",
      "type": "integer",
      "minimum": 1066
    },
    "end_year": {
      "title": "End year",
      "description": "If this court no longer publishes documents, the default end year of the document holding range for this court in Find Case Law.",
      "type": "integer"
    },
    "show_to_editors": {
      "title": "Show to editors",
      "description": "Should this court be visible to editors?",
      "type": "boolean"
    },
    "show_in_search_filters": {
      "title": "Show in search filters",
      "description": "Should this court appear as an advanced search filter option?",
      "type": "boolean"
    },
    "show_in_public_directory": {
      "title": "Show in courts and tribunals directory",
      "description": "Should this court appear in the public directory of courts and tribunals?",
      "type": "boolean"
    },
    "ended": {
      "title": "Ended",
      "description": "Has the court ended?",
      "type": "boolean"
    },
    "court_of_record": {
      "title": "Court of record",
      "description": "Is this court a court of record?",
      "type": "boolean"
    },
    "jurisdictions": {
      "type": "array",
      "items": {
        "title": "Raw Jurisdiction",
        "type": "object",
        "properties": {
          "prefix": {
            "type": "string"
          },
          "name": {
            "type": "string"
          },
          "code": {
            "type": "string"
          }
        },
        "required": ["prefix", "name", "code"],
        "additionalProperties": false
      }
    },
    "relationships": {
      "title": "Relationships",
      "description": "Other courts which this court relates to.",
      "type": "array",
      "items": {
        "title": "Court relationship",
        "description": "An object describing the relationship between two courts.",
        "type": "object",
        "properties": {
          "court_code": {
            "$ref": "./court.schema.json#/$defs/CourtCode"
          },
          "relationship_type": {
            "type": "string",
            "enum": [
              "hears_appeals_from",
              "hears_similar_cases_to",
              "might_be_looking_for"
            ]
          }
        },
        "required": ["court_code"]
      }
    }
  },
  "required": [
    "code",
    "name",
    "link",
    "show_to_editors",
    "show_in_search_filters",
    "show_in_public_directory"
  ],
  "dependentRequired": {
    "end_year": ["start_year"],
    "ncn_pattern": ["ncn_examples"],
    "ncn_examples": ["ncn_pattern"]
  },
  "additionalProperties": false,
  "allOf": [
    {
      "if": { "properties": { "show_in_search_filters": { "const": true } } },
      "then": { "required": ["param"] }
    },
    {
      "if": { "properties": { "show_in_public_directory": { "const": true } } },
      "then": { "required": ["param"] }
    },
    {
      "if": { "properties": { "show_to_editors": { "const": true } } },
      "then": { "required": ["param"] }
    }
  ]
}
